console.log("ksjfksjfksfkfks")
$("body").append("<%= escape_javascript render 'products/product_quick_view', product: @product %>")

// Setup event for elements
setupInputQty()
// Setup Input Form
setupFormAddToCart(<%= @product.id %>)

document.getElementById("dock-add-to-cart").style.cssText = "display: none !important"

$('#product-quick-view').click(function (e) {
    const container = $('#product-quick-view')

    if (container.is(e.target) // if the target of the click isn't the container...
        && container.has(e.target).length === 0) // ... nor a descendant of the container
    {
        container.remove();
    }
})